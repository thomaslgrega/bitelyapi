name: Hook tests

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run hook tests
        run: bash .claude/hooks/codex-pr-review_test.sh
